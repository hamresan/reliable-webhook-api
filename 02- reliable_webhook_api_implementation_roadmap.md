# Reliable Webhook API — Implementation Roadmap

This file mirrors the implementation roadmap used for the staged development of this repository.

## Stage 0 — Project foundation and developer experience

- Create the project foundation under a `src/` layout.
- Configure Python 3.12, FastAPI, SQLAlchemy async support, Alembic, pytest, Ruff, Pyright, and Docker.
- Add a minimal application factory and `GET /health`.
- Add CI and baseline quality commands in the Makefile.
- Keep README commands honest and limited to implemented behavior.

See the full roadmap maintained with the project planning materials for later stages.
